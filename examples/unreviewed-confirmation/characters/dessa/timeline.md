---
character_id: dessa
name: Dessa Rill
edit_policy: editable
---

# Dessa Rill Timeline

<!-- ILLUSTRATIVE EXAMPLE — NOT REAL PROJECT CANON OR STATE. See
     examples/unreviewed-confirmation/README.md.

     One capture-agent-style invention (fact-type `event`) from an earlier, unmodeled
     drafting pass — scene00/beat01/attempt01, before this fixture's own scene01 draft.
     The drafting apparatus that produced it isn't built here; only the resulting file is,
     per the task's own instruction. Shown in its POST-confirmation state: a direct,
     confirm-only /revise pass flipped the inline tag with no content change, invoked
     independent of any compliance finding. See the README for the before state and the
     worked /revise invocation. -->

## Events

### Apprenticed to a salt-diviner in Drybone Market
- **id:** tl-01
- **story-position:** pre-story
- **committed-in:** —
- **event:** Before joining Torrin's crew, Dessa spent a season apprenticed to a
  salt-diviner in Drybone Market, learning to read wind-scour patterns for weather sign.
  (invented, confirmed — scene00, beat01, attempt01)
- **notes:** Captured under Rule 1 as a non-load-bearing background detail; confirmed
  directly via /revise, independent of any compliance finding — `compliance_report` has
  no input reading `timeline.md`/`profile.md` (ROADMAP.md's Deferred → Now-actionable
  list: "`compliance_report` consultation of `characters/<id>/timeline.md` /
  `profile.md` for unreviewed-premise detection").

---

## Corrected or superseded

<!-- No transitions in this fixture. -->
