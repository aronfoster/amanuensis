---
character_id: dessa
name: Dessa Rill
edit_policy: editable
---

# Dessa Rill — Timeline

<!-- ILLUSTRATIVE EXAMPLE — NOT REAL PROJECT CANON OR STATE. See
     examples/unreviewed-confirmation/README.md.

     Real capture-agent output for timeline.md is a flat, provenance-tagged bullet list
     under loose chronological headers — no id / story-position / committed-in scaffold
     (that structured shape is templates/timeline.md's documented form for the fuller
     M14 temporal model; capture-agent.md's own Write discipline for this destination is
     just "an inline note or trailing tag riding alongside the new entry"). Shown here in
     its POST-confirmation state: a direct, confirm-only /revise pass flipped the inline
     tag with no content change, invoked independent of any compliance finding, since
     compliance_report has no input reading timeline.md/profile.md at all (ROADMAP.md's
     Deferred -> Now-actionable list carries this gap by name). See the README for the
     before state and the worked /revise invocation. -->

> One capture-agent-style invention (fact-type `event`), captured from drafting under
> Rule 1 as a non-load-bearing background detail. A human reviewer confirms entries
> before downstream steps rely on them.

## Pre-story

- Before joining Torrin's crew, spent a season apprenticed to a salt-diviner in Drybone
  Market, learning to read wind-scour patterns for weather sign — the habit that shows
  in how she reads the ridge on the way down at scene 1, beat 4. *(invented, confirmed —
  scene 1, beat 4, attempt01)*
